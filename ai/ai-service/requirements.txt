fastapi==0.115.0
uvicorn==0.30.0
anthropic>=0.92.0
httpx==0.27.0
pydantic==2.13.5
python-jose==3.3.0
PyJWT==2.8.0
psycopg2-binary==2.9.12
pgvector==0.3.2
python-dotenv==1.0.0
mcp==1.1.2
voyageai==0.5.0
prometheus_client==0.26.0

# Часовые пояса для инструментов календаря (services/tools/datetime_tools.py).
# В Linux-образе база IANA уже есть, но у CPython под Windows её нет, и
# ZoneInfo("Europe/Berlin") падает с ZoneInfoNotFoundError. Нужен для локальной
# разработки; в контейнере безвреден.
tzdata>=2026.1

# litestar==2.24.0
# Отключён намеренно. Он попал в проект вместе с фрагментом туториала в main.py:
# файл создавал приложение Litestar, а затем вызывал на нём методы FastAPI
# (add_middleware / include_router) и ссылался на несуществующее имя `root` —
# сервис падал при импорте. Все роутеры написаны на FastAPI, второй веб-фреймворк
# ради одной метрики не нужен: Prometheus подключён напрямую через
# prometheus_client. Строка оставлена закомментированной, а не удалена, чтобы
# решение было видно в diff.
